export type SiteMetadata = {
  title: string
  author: {
    name: string
    summary: string
  }
  description: string
  siteUrl: string
  social: {
    twitter: string
    github: string
  }
  repository: string
}